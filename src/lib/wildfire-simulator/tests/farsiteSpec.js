let mapSpecs = {
    width: 512,
    height: 512,
    ftPerPixel: 1,
    centerEast: 0,
    centerNorth: 0
}
const fireMap = makeFireMap(mapSpecs)
setUnburnableCells(fireMap)
setB

export function setUnburnableCells(fireMap) {
}