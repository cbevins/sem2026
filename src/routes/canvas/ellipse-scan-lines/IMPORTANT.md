Use just the following files:

Use scanEllipseV1.js as it has been tested, optimized,
and is independent of any class or object structure
(i.e., it runs on just its function arguments).

Use LightweightFireEllipse.js as a fast, light object builder
that uses fire behavior parameters to construct a Cartesian ellipse.
